<Window x:Class="DicomReceiver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DicomReceiver.ViewModels"
        xmlns:helpers="clr-namespace:DicomReceiver.Helpers"
        Title="{Binding Title}"
        Width="1100" Height="700"
        MinWidth="900" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Closing="Window_Closing">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="150" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="8,6">
            <DockPanel>
                <!-- Left: SCP controls -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Content="{Binding ToggleButtonText}"
                            Command="{Binding ToggleScpCommand}"
                            Style="{StaticResource AccentButton}"
                            Width="100" FontSize="13" FontWeight="SemiBold" />

                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,2" />

                    <Button Command="{Binding OpenSettingsCommand}"
                            Style="{StaticResource DarkButton}"
                            FontSize="13" Padding="8,4" ToolTip="Settings">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,5,0" />
                            <TextBlock Text="Settings" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Right: Delete all -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Delete All"
                            Command="{Binding DeleteAllCommand}"
                            Style="{StaticResource DarkButton}"
                            FontSize="12" Padding="10,4" />
                </StackPanel>

                <StackPanel /> <!-- spacer -->
            </DockPanel>
        </Border>

        <!-- Study Queue -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Studies}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Background="{StaticResource BackgroundBrush}"
                  Foreground="{StaticResource TextPrimaryBrush}"
                  RowBackground="{StaticResource BackgroundBrush}"
                  AlternatingRowBackground="{StaticResource SurfaceBrush}"
                  BorderBrush="{StaticResource BorderBrush}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                  HeadersVisibility="Column"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  FontSize="13"
                  Margin="0">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                    <Setter Property="Padding" Value="8,6" />
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                    <Setter Property="BorderThickness" Value="0,0,1,1" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="12" />
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,4" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#33FFFFFF" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <!-- Status indicator -->
                <DataGridTemplateColumn Header="" Width="32" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="12" Height="12" HorizontalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#FFA726" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Receiving">
                                                <Setter Property="Fill" Value="#FFA726" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Complete">
                                                <Setter Property="Fill" Value="#0F9B58" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Burning">
                                                <Setter Property="Fill" Value="#2196F3" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Done">
                                                <Setter Property="Fill" Value="#4CAF50" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                                <Setter Property="Fill" Value="#E53935" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Patient" Binding="{Binding PatientName}" Width="200" />
                <DataGridTextColumn Header="Study Date" Binding="{Binding StudyDate}" Width="100" />
                <DataGridTextColumn Header="Modality" Binding="{Binding Modality}" Width="80" />
                <DataGridTextColumn Header="Series" Binding="{Binding SeriesCount}" Width="60" />
                <DataGridTextColumn Header="Images" Binding="{Binding ImageCount}" Width="70" />
                <DataGridTextColumn Header="Size" Binding="{Binding TotalSizeFormatted}" Width="90" />
                <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="200" />

                <!-- Action buttons -->
                <DataGridTemplateColumn Header="" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Burn"
                                        Command="{Binding DataContext.BurnStudyCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource AccentButton}"
                                        Padding="8,2" FontSize="11" Margin="0,0,4,0"
                                        IsEnabled="{Binding Status, Converter={StaticResource StatusToBoolConverter}}" />
                                <Button Content="X"
                                        Command="{Binding DataContext.DeleteStudyCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DarkButton}"
                                        Padding="6,2" FontSize="11" Foreground="#E53935" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Splitter -->
        <GridSplitter Grid.Row="2" Height="4" HorizontalAlignment="Stretch"
                      Background="{StaticResource BorderBrush}" />

        <!-- Log Panel -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <DockPanel>
                <DockPanel DockPanel.Dock="Top" Margin="8,4">
                    <TextBlock Text="Log" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                               FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Button Content="Clear" Command="{Binding ClearLogCommand}"
                            Style="{StaticResource DarkButton}"
                            FontSize="10" Padding="6,2" HorizontalAlignment="Right" />
                </DockPanel>
                <ListBox ItemsSource="{Binding LogEntries}"
                         Background="Transparent"
                         Foreground="{StaticResource TextSecondaryBrush}"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="11"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         x:Name="LogListBox">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,1" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="4" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="10,4">
            <DockPanel>
                <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#E53935" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                    <Setter Property="Fill" Value="#0F9B58" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding StatusText}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" VerticalAlignment="Center" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
